// Temporal bridge: preserve legacy path for the maintenance module while the new
// feature-first page lives under `features/maintenance`.
export { default } from '../../features/maintenance/pages/MaintenancePage';